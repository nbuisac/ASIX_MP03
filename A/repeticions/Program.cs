int taula;
int comptador;
// taula = Convert.ToInt32(Console.ReadLine());
comptador = 2;
while (comptador <= 20) {
    Console.WriteLine($"{comptador}");
    comptador=comptador + 2; // Recordeu que cal anar modificant el comptador
}